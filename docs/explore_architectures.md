## 1. An agent file with referenced files CLAUDE.md

Observations:

- Agent did not successfuly logged in and as a result of that it went on to read unrelated files like `CHALLENGES.md`
- Agent keep trying to call all the world directions, south, north, east, west, with sleep 1|2|3
- Agent created a script like `explore_south.sh`, `explore_general_store.sh` in `/private/tmp`
- Agent did not update the `data/player.md` and `data/world.md` files with the new actions and observations.
- Switching to Sonnet 5 didn't help at all, model startet to create python scripts, hit tool errors, was working for ~3mins with no progress.

### Technical Conclusion

A coding harness alone is not a good fit for operating the MUD.

The deterministic login and connection flow should be handled by a
reusable MUD SDK rather than regenerated by the model on every attempt.

A specialized agent loop, potentially using an MCP server connected to
the MUD SDK, would provide better reliability, visibility, and token
efficiency.

## 2. Agent Skills driven by main agent

Skills are a files that describe reusable actions that can be used by the main agent.

- Created deamon in python for the game and a `send` shell script to send commands to the game. Agent also looked at 001_plain_agent directory and mirrored the data directory structure.
- Overall the agent is doing substianlty better, it reached Midgaard
- Still not updating the `data/player.md` and `data/world.md` files with the new actions and observations.
- It reached the gate in Midgaard, but was not able to go past the gate. Decided to wait. "Gate's still closed at 1am. I'll poll in the background until it opens (likely around 6am game-time) rather than blocking here."
  "I'll wait for this background poller to notify me once the Midgaard gate opens (it retries every 15s, up to ~7.5 minutes, since the gate is closed for the night)." I love this.
- Agent took "Players strating guild" as the guild of swordsmen and it successfully practiced kicking. After that it actually updated `player.md` and `world.md` files with the new actions and observations.
- Haiku was not able to find the Minotaur, it wandered into zones above player level. Player was hungry, exhausted and had to rest.
